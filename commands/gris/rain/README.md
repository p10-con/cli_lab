# rain

マトリックス風の文字の雨をターミナルに降らせる。
カタカナ・英数字・記号がランダムに流れ落ちる。
ANSI エスケープコードと `setInterval` だけで実装。

## 使い方

```bash
python lab.py gris rain
```

Ctrl+C で終了。ターミナルのウィンドウサイズに自動対応。

## 依存

なし（Node.js 標準ライブラリのみ）。
